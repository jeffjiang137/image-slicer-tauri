{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to use core APIs, dialog APIs, and local application commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "app-commands"
  ]
}
